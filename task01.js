// Our array of values
/*

// TASK -1
const myArray = ["T", "H", "R", "E", "E"];

const countDuplicates = (arr) => {
  const counts = {};

  arr.forEach((value) => {
    if (!counts[value]) {
      counts[value] = 1;
    } else {
      counts[value]++;
    }
  });
  return counts;
};
const result = countDuplicates(myArray);

console.log(result);
*/
// TASK -2
for (let i = 0; i <= 8; i++) {
  for (let j = 1; j < 9; j++) {
    for (let k = 6; k <= 8; k++) {
      console.log("hii");
    }
  }
}
// NO.OF THE ITERATION IS 216 times
